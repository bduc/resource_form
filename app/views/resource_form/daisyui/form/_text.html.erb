<%= f.text_field name,
      class: "input input-sm w-full #{'input-error' if error} #{'join-item' if spec[:prepend] || spec[:append]}",
      placeholder: spec[:placeholder],
      required: spec[:required],
      readonly: spec[:readonly],
      disabled: spec[:disabled] %>
